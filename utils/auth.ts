export const AUTH_STORAGE_KEYS = {
  accessToken: "nppweb.accessToken",
  refreshToken: "nppweb.refreshToken",
  user: "nppweb.user"
} as const;

export const LEGACY_AUTH_STORAGE_KEYS = {
  accessToken: "aimsora.accessToken",
  refreshToken: "aimsora.refreshToken",
  user: "aimsora.user"
} as const;
